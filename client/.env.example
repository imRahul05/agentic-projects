# Origin of the weather agent API (the Express server in ../server).
# Required: the client has no fallback and will surface a configuration error
# instead of guessing an origin. Read at build time — see lib/api/http.ts.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
